@using BusinessLogic;
@model BusinessLogic.DataModels.Customers

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AllCustomers[0].Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AllCustomers[0].Age)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AllCustomers[0].Balance)
        </th>
        <th></th>
    </tr>
@if (Model.AllCustomers != null) { 
foreach (var item in Model.AllCustomers) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Age)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Balance)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}
    }
</table>
